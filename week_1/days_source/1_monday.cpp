#include "1_monday.h"
#include "pch.h"

/*
	Day Description:
		Today, it's operators. 
		The math operators ( +, /, -, etc.) 

	How to use a feature:
		add			=>	+	=>	a + b	=> sum of a and b
		subtract	=>	-	=>	a - b	=> a take b
		divide		=>	/	=>  a / b	=> a divided by a b
		multiply	=>	*	=>	a * b	=> a times b
		modulo		=>	%	=>	a % b	=> divide a by b, and give the remainder
*/

/*	
	Add value to source,
	then return it.
*/
int add(int source, int value) {
	return 0;
}

/*
	Take value from source
	then return it.
*/
int subtract(int source, int value) {
	return 0;
}

/*
	divide source by value.
	then return it.
*/
int divide(int source, int value) {
	return 0;
}

/*
	Divide source by the value
	then return it.
*/
int remainder(int source, int value) {
	return 0;
}

/*
	multiply source by value,
	then return it.
*/
int multiply(int source, int value) {
	return 0;
}